[project]
name = "division-equivalence"
version = "0.1.0"
description = "Hypothesis test for division and reciprocal multiplication equivalence"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "hypothesis>=6.155.2",
    "pytest>=9.0.3",
]
